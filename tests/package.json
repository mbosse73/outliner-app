{
  "name": "outliner-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-Pruefungen fuer index.html — reines Entwicklerwerkzeug, nicht Teil der App selbst.",
  "scripts": {
    "test": "node alle.js"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
